# Implementation Plans

Generated by the improve skill on 2026-07-25 against commit `5e31904`. Execute in the order below unless dependencies say otherwise. Each executor must read its plan fully, honor its STOP conditions, run every verification command, and update its status row when done.

## Execution order & status

| Plan | Title                                                     | Priority | Effort | Depends on | Status |
| ---- | --------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Render live energy values without an energy-period window | P1       | S      | —          | TODO   |
| 002  | Make template subscriptions reactive and fully disposable | P1       | M      | 001        | TODO   |
| 003  | Keep home-ring circumferences finite at zero consumption  | P1       | S      | 001, 002   | TODO   |
| 004  | Honor the configured energy flow-rate model               | P1       | S      | 003        | TODO   |
| 005  | Make CI verify the web app and shared UI package          | P1       | M      | —          | TODO   |

Status values: TODO, IN PROGRESS, DONE, BLOCKED with a one-line reason, or REJECTED with a one-line rationale.

## Dependency notes

- Plan 001 lands first because Plans 002-004 all touch the energy card and its render test. Its render-placeholder change should not alter their quoted subscription, circumference, or configuration excerpts.
- Plan 002 follows Plan 001 and establishes lifecycle tests before later edits continue in both flow-card files.
- Plan 003 follows Plans 001 and 002 to avoid parallel edits to both card implementations and render suites. It has no runtime dependency on their behavior.
- Plan 004 follows Plan 003 because both modify Energy Flow Card Plus configuration/render coverage.
- Plan 005 is independent and may run in parallel in an isolated worktree. Keep it separate from card fixes because it changes repository-wide CI gates.
- Every plan was written against `5e31904`. When an earlier plan changes an in-scope file, later executors must run the drift check and verify that their quoted excerpt remains intact before proceeding.

## Verification baseline

At the planned commit:

- `pnpm typecheck` exits 0, but Turbo skips `@flixlix-cards/web` and `@flixlix-cards/ui` because they lack `typecheck` scripts.
- `pnpm test` exits 0 for the shared and card packages but explicitly excludes `@flixlix-cards/web`.
- `pnpm --filter @flixlix-cards/web exec tsc --noEmit` exits 0.
- `pnpm --filter @flixlix-cards/web build` exits 0.
- `pnpm --filter @flixlix-cards/ui exec tsc --noEmit` fails with four known errors documented in Plan 005.
- The audit environment used Node 22.14.0 and emitted an engine warning because the repository requires Node 22.18.0 or newer. Executors should use `.nvmrc`.

Before executing any plan, run `node --version`. It must report 22.18.0 or newer; otherwise activate the version from `.nvmrc` and rerun the check before any package command.

## Deferred findings not selected for plans

- Refresh the dependency graph and reconcile current critical/high advisories with open PRs #301 and #316.
- Add direct tests for energy-period discovery, listener patching, and statistics requests in `packages/shared/src/states/utils/energy-period.ts`.
- Correct the shared Vitest input path in `turbo.json`.
- Remove or constrain the unpinned third-party analytics script and add deployment response headers.
- Stop advertising absent configurator routes, or implement the promised configurator as a product initiative.
- Consolidate the duplicated power and energy flow-card controllers after correctness fixes and PR #300.
- Align `CONTRIBUTING.md` with the exact Node engine requirement.
- Decide whether to rebase or retire the Energy Period Selector Plus PR #19.

## Findings considered and rejected

- Recomputing render data on unrelated Home Assistant state updates was not replanned because open PR #300 directly implements and tests that optimization.
- Legacy Lit 2 imports were not replanned because open PR #298 removes them.
- Flow-editor duplication was not replanned because open PR #302 extracts the shared editor base.
- Root card-list and issue-template drift was not replanned because open PR #305 addresses it; its issue-labeler follow-up remains deferred.
- Missing agent guidance was not replanned because open PR #303 adds the repository guide.
- Energy Period Selector Plus was not classified as nonexistent after open PR #19 confirmed an implementation exists, although that PR is stale and conflicting.
- Embedding the distribution token in an ephemeral GitHub Actions clone URL was not promoted as a standalone vulnerability because runner isolation and secret masking materially reduce the claimed exposure; least-privilege release authentication remains worthwhile hardening.
- Energy Breakdown Card's changelog/package version mismatch was not promoted without confirming whether the changelog entry intentionally represents an unreleased version.
