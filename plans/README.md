# Implementation Plans

Generated by the improve skill on 2026-06-13 at commit `ecfffc3` (branch `fix/sortable-list-animate`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

All findings were verified by the advisor against the cited code before planning (subagent reports were vetted; corrections and rejections listed at the bottom).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix negative-flow tolerance handling | P1 | S | — | DONE (worktree `worktree-agent-a54d925d01a5785cf`, unmerged) |
| 002 | Harden CI (frozen lockfile, typecheck/lint gates, concurrency) | P1 | S | — | DONE (worktree `agent-aa2534c246fe2d070`, branch `worktree-agent-aa2534c246fe2d070`, unmerged) |
| 003 | Purge Lit 2 from packages/shared | P1 | S | — | DONE (worktree `agent-a93c02d6192a58b98`, branch `worktree-agent-a93c02d6192a58b98`, unmerged; bundle −6.1%) |
| 004 | Real `_computeRenderData` tests for both flow cards | P1 | M | 001 | DONE (worktree `exec-004`, branch `advisor/004-card-render-tests`, unmerged; reviewed — 10+10 tests pass, typecheck clean, scope clean) |
| 005 | Skip renderData recompute on irrelevant hass updates | P2 | M | 004 | DONE (branch `advisor/005-skip-recompute`, **stacked on `advisor/004`**, unmerged; reviewed — 78 shared + 10/10 card tests pass, willUpdate-only edits, scope clean) |
| 006 | Clear high-severity build-chain advisories | P2 | M | — (002 first recommended) | TODO |
| 007 | Extract shared `BaseCardEditor` (flow cards) | P3 | L | 003, 004 | TODO |
| 008 | Write root CLAUDE.md | P2 | S | — | DONE (branch `worktree-agent-a2b8573f5614d7e06`, unmerged; reviewed + revised — removed premature BaseCardEditor convention; 98 lines, facts verified) |
| 009 | Monorepo hygiene batch (packageManager pins, stale glob, dup constant, dead dep, vitest drift) | P2 | S | — (sequence with 003/006) | TODO |
| 010 | Fix docs drift (README card list, issue templates, CONTRIBUTING ports) | P2 | S | — | DONE (branch `worktree-agent-a175548a1a9da4db4`, unmerged; reviewed — YAML valid, links resolve, scope clean. Follow-up: `issue-labeler.yaml` labelMap still maps the removed card) |
| 011 | Investigate/cap template topics for >4 individual devices | P3 | S | — | TODO |
| 012 | Decision brief for stalled `feat/add-power-pulse-card` branch | P3 | S | — | DONE (brief at `plans/012-power-pulse-report.md`; recommendation: **Revive** — core impl complete, 4 finishing touches missing, zero drift conflicts; branch tip `8fd3e78`) |
| 013 | Showcase the UI editor in the docs (power-flow first) | P3 | M | — | TODO |
| 014 | Quantize flow-dot durations to stop SMIL resync churn | P2 | S | — | DONE (branch `worktree-agent-ae998f959a06bebc4`, unmerged; reviewed — 57 shared tests pass, scope clean, individual flow rate untouched) |
| 015 | Pause flow animations off-screen + honor prefers-reduced-motion | P2 | M | — (sequence with 005/016) | TODO |
| 016 | Cut per-render micro-costs (cached number formatter, ResizeObserver hygiene) | P3 | S | — (sequence with 005/015) | DONE (branch `worktree-agent-a4fa85efc15b91ecf`, unmerged; reviewed — 57 shared + 5 + 7 card tests pass, getBoundingClientRect removed, scope clean) |
| 017 | Replace SMIL dots with CSS Motion Path | P3 | L | 004, 014 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **004 requires 001**: one regression test asserts the fixed negative-individual behavior.
- **005 requires 004 DONE**: the render-guard change is the riskiest behavioral edit; the renderData test suite is its safety net.
- **007 requires 003** (same shared ui-editor files; avoids conflicting edits) **and 004** (general net under editor-adjacent refactoring).
- **006 after 002** (recommended): a frozen-lockfile CI verifies the dependency bumps the way developers experience them.
- **009 conflicts softly with 003 and 006**: all three touch `pnpm-workspace.yaml` and/or `packages/shared/package.json`. Run sequentially, any order.
- **013 touches `apps/web`**, which carried uncommitted maintainer work at planning time — its drift check coordinates this.
- **014–017 added 2026-06-13** from a maintainer-requested follow-up on animation/render performance (low-end devices); findings verified in-session against the cited code, not from a fresh audit.
- **005, 015, 016 all edit the same two card files** (different methods: `willUpdate` vs. lifecycle/`updated`). No hard dependency, but run them **sequentially** in any order to avoid merge conflicts; 005 first is recommended (its guard reduces how often the others' code paths run at all).
- **017 requires 004** (render tests are the only automated net for an animation-layer rewrite) **and 014** (quantized durations make 017's phase-preservation a rare path instead of a per-tick one). **017 Step 5 ports 015's pause mechanism** — if 015 lands after 017 instead, 015's Steps 3–4 must use `animation-play-state`, not `pauseAnimations()` (015's STOP conditions cover this).
- **014 before 015 recommended**: fewer duration-resync events means fewer pause/unpause interplay edge cases to reason about.

## Findings considered and rejected

- **"Division by zero in `compute-flow-rate.ts`"** — already guarded (`Number.isFinite` fallback at line 39); silent-fallback cosmetics only. Folded into Plan 004's test scope.
- **"`adjustZeroTolerance` mishandles small positive values"** — wrong as reported (0.5 below tolerance 5 is correctly zeroed); the real defect was negative values → became Plan 001.
- **"JSON.parse deep-clone fragility in `compute-individual-position.ts`"** — speculative; the cloned objects are plain data today. Not worth doing.
- **"DIST_REPO_TOKEN exposed in clone URL (`release.yml:282`)"** — downgraded: GitHub Actions masks secret values in logs; residual exposure (token in the runner's git remote config) is low for this workflow. Not planned.
- **Demo-video preload optimization (`apps/web/public/videos`)** — ≤700KB worst case; not worth a plan.
- **"hero-flow.tsx integration" (direction)** — active uncommitted maintainer work, not an audit finding.
- **`@tanstack/*: latest` + `nitro` beta pins** — real debt, but re-pinning is a docs-site stability decision; recorded as deferred in Plans 006/009 rather than planned unilaterally.
- **`custom-card-helpers` abandonment** — MED confidence, ~6 import sites (types, `fireEvent`, `debounce`); migration deserves its own plan once a replacement type source is chosen. Deferred — see Plan 009 maintenance notes.

## Audit coverage note

Standard-depth audit: all nine playbook categories over the card packages, shared package, tooling, CI, and docs site. Not audited in depth: `apps/web` route-level bundle composition (needs a build/waterfall analysis), the release workflow's dist-repo scripts beyond secret handling, and `packages/ui` component quality.
